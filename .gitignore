# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# ROS2
build/
install/
log/

# Docker
docker/*.log

# MuJoCo
MUJOCO_LOG.TXT

# Large raw video captures (keep only final compressed .mp4 versions)
# Applies anywhere in the repo, not just videos/, so a raw screen recording
# dragged into the project root can't get committed by accident (GitHub
# hard-blocks any file over 100MB anyway).
*.mov
Milestone 01/videos/raw_*
